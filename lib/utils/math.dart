double calculatePercentageChange(double v1, double v2) {
  //calculates the percentage change from v1 to v2
  return ((v2 - v1) / v1.abs()) * 100;
}

double calculateAverage(List<num> listOfNumbers) {
  return 0;
}